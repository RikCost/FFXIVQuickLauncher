* Fixed launcher language theme colors so each language now uses a dedicated country flag-inspired palette instead of the wrong fallback colors. (by goaaats)
* Fixed local/non-official builds to skip production auto-update checks, improving first-run testing stability after installation. (by goaaats)
* Added a Russian README and linked it from the main README while keeping existing links for other languages. (by goaaats)
